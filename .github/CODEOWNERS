# CODEOWNERS — Maia v2 repository.
#
# Architecture Locks (master spec §0.1) — these files encode invariants
# that require explicit founder approval to change. PRs that modify them
# MUST receive a review from the founder before merge.
#
# P10a — Knowledge State Machine (master §0.1 "Knowledge lifecycle"):
#   - transitions.ts holds ALLOWED_TRANSITIONS (the 9-state graph).
#   - visibility.ts holds VISIBILITY_TABLE (what the LLM ever sees).
#   - state-machine.ts holds decideInitialStatus + the propose contract.
#     Changing thresholds (1/24h, 3/30d, 7/90d, 0.9/10, 30d/90d TTL)
#     also counts as a lock change.
src/control-plane/knowledge-state-machine/transitions.ts  @diogenes-mendes
src/control-plane/knowledge-state-machine/visibility.ts   @diogenes-mendes
src/control-plane/knowledge-state-machine/state-machine.ts @diogenes-mendes
src/control-plane/knowledge-state-machine/types.ts        @diogenes-mendes
src/workers/knowledge-state-promoter.ts                   @diogenes-mendes
